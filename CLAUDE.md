# Wiki 维护规则

本项目在 `wiki/` 下维护基于 docsify 的使用手册，服务内通过 `/wiki/` 路径访问（`server.mjs` 的 `serveWiki` 提供，免登录）。

**新增或修改任何用户可见功能时（页面、图表、设置项、交互、告警、后台自动化行为等），必须同步更新 `wiki/` 对应章节：**

- 新功能：在对应页面加小节，必要时新建页面并在 `wiki/_sidebar.md` 挂目录
- 行为变更 / 界面变化：修改对应描述，删掉已不存在的内容
- wiki 内容面向**使用者**（不是开发者）：写「是什么、怎么用、在哪里」，不写实现细节
- **深度要求**：只写功能清单不够。涉及参数/算法的功能必须写到「参数默认值 + 计算逻辑 + 边界情况」层面——从代码提取真实默认值与公式（不凭印象写），参数用表格，公式用伪代码块，每个核心机制配数字代入示例，坑写成明确的「⚠️ 注意」条目。机制类细节集中在 `mechanism-quota.md` / `mechanism-proxy.md` / `metrics-reference.md` 三页，功能页链接过去
- 时间口径统一写北京时间（UTC+8）
