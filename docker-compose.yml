services:
  cc-team:
    build: .
    container_name: cc-team
    ports:
      - "6789:6789"
    volumes:
      - ./config.json:/app/config.json
      - ./data.json:/app/data.json
    restart: unless-stopped
